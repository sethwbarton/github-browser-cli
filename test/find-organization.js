'use strict'

const { expect } = require('chai')
const { describe, it, beforeEach } = require('mocha')
const sinon = require('sinon')

describe('GitHub Group Lookup', () => {
  it('Returns information about a GitHub group', () => {

  })
})